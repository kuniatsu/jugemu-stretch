import { HashRouter } from 'react-router-dom'
import { AppNavigator } from './navigation/AppNavigator'

function App() {
  return (
    <HashRouter>
      <AppNavigator />
    </HashRouter>
  )
}

export default App
